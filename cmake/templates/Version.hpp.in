#pragma once

#include <string>

#ifdef _WIN32
  #define ATE_VERSION_API __declspec(dllexport)
#else
  #define ATE_VERSION_API
#endif

namespace ate::version {

std::string full();
std::string git_hash();

} // namespace ate::version

extern "C" {
    ATE_VERSION_API const char* ate_version_full();
    ATE_VERSION_API const char* ate_version_git_hash();
}